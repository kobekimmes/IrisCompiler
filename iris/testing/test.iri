# This is a single-line comment, which is going to be ignored hopefully by the lexer

num x = (10+2112233);

return hello;

function kobe(word name) -> none { # hello there is a comment here as well
    num y = 10;
    y == 20;
}